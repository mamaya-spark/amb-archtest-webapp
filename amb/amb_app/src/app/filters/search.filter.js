function searchFilter(lodash) {
    'ngInject';

    return (data, query) => {
        let filtered = data;

        if(query) {

        }

        return filtered;
    }
}

export default searchFilter;
